name: everest_flutter_crypto_tests
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  mocktail: ^0.3.0
  json_serializable: ^6.4.1
  json_annotation: ^4.7.0
  dio: ^4.0.6
  intl: ^0.17.0
  fl_chart: ^0.55.2
  lottie: ^1.4.3
  flutter_hooks: ^0.18.5+1
  flutter_spinkit: ^5.1.0
  decimal: ^2.3.0
  hooks_riverpod: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.2.1


  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/img/
